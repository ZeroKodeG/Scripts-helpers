services:
  backend:
    build:
      context: ./server
    container_name: auditoria-backend
    restart: unless-stopped
    env_file:
      - ./server/.env
    environment:
      - PORT=3000
      - DB_PATH=./data/auditoria.db
    volumes:
      - ./server/data:/app/data
